[phases.setup]
nixPkgs = ['...', 'python']

[phases.install]
cmds = ['mkdir -p /var/log/nginx && mkdir -p /var/cache/nginx', 'composer install --ignore-platform-reqs', 'npm install --python=python2.7']

[phases.build]
cmds = ['php artisan config:cache', 'php artisan route:cache', 'php artisan migrate --force', 'npm run prod']